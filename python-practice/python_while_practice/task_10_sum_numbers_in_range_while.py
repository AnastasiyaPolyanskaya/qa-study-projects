## Task 10 - Сумма чисел в диапазоне (цикл `while`)

**Условие:**

На вход принимаются **два числа**:

1. число `a` — начало диапазона;
2. число `b` — конец диапазона.

С помощью цикла `while` необходимо вычислить **сумму всех чисел в диапазоне от `a` до `b` (включительно)**.

Программа должна корректно работать **для любой пары значений `a` и `b`**, где `a ≤ b`.

---

### Решение

```python
a = int(input())
b = int(input())

total = 0
current = a

while current <= b:
    total += current
    current += 1

print(total)


