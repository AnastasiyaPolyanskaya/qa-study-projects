# Задача 2 - Напишите код, который на входе принимает список с числами, через пробел.
Выведите на печать только нечетные числа, используя оператор continue.

numbers = list(map(int, input().split()))
for i in numbers:
    if i % 2 == 0:
        continue
    print(i)
