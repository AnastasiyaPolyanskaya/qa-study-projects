# Задача 1 - Напишите код, который на входе принимает список с числами, через пробел.
Выведите на печать первое отрицательное число в списке, используя оператор break.

numbers = list(map(int, input().split()))

for x in numbers:    
    if x < 0:      
        print(x)     
        break   
