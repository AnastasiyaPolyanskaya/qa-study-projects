# Задача 3 - Напишите код, который на входе принимает список с числами, через пробел.
Подсчитайте количество положительных и отрицательных чисел в списке, 
и выведите на печать сперва количество положительных, затем отрицательных, используя операторы break и continue.

numbers = list(map(int, input().split()))

positive_count = 0
negative_count = 0

for num in numbers:
    if num == 0:
        continue  
    if num > 0:
        positive_count += 1
    else:
        negative_count += 1

print(positive_count)
print(negative_count)
