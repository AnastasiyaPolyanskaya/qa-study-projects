# Учебный скрипт: несколько операций со строками в одном месте

text = input("Введите строку: ")

# 1. Вывод строки без изменений
print("1) Строка целиком:", text)

# 2. Вывод второго символа
if len(text) >= 2:
    print("2) Второй символ:", text[1])
else:
    print("2) Второго символа нет (строка короче 2 символов)")

# 3. Первые четыре символа
print("3) Первые четыре символа:", text[:4])

# 4. Символы с чётными индексами (0, 2, 4, ...)
print("4) Символы с чётными индексами:", text[0::2])

# 5. Предпоследний символ
if len(text) >= 2:
    print("5) Предпоследний символ:", text[-2])
else:
    print("5) Предпоследнего символа нет (строка слишком короткая)")

# 6. Последнее слово с конца строки
words = text.split()
if words:
    last_word = words[-1]
    print("6) Последнее слово:", last_word)
else:
    print("6) В строке нет слов")

# 7. Удаление заданного символа из строки
ch = input("7) Введите символ, который нужно удалить из строки: ")
print("7) Строка без этого символа:", text.replace(ch, ""))

# 8. Работа с регистром: первая строка в ВЕРХНИЙ, вторая в нижний
second_text = input("8) Введите вторую строку для работы с регистром: ")
print("8) Разные регистры:", text.upper(), second_text.lower())
